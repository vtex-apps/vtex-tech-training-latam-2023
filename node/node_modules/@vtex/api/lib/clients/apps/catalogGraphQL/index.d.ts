import { AppGraphQLClient } from '..';
import { InstanceOptions, IOContext } from '../../..';
import { Brand } from './brand';
import { Category } from './category';
import { Product } from './product';
import { SKU } from './sku';
export declare class CatalogGraphQL extends AppGraphQLClient {
    constructor(ctx: IOContext, opts?: InstanceOptions);
    sku: (id: string) => Promise<{
        sku: SKU;
    } | undefined>;
    product: (id: string) => Promise<{
        product: Product;
    } | undefined>;
    category: (id: string) => Promise<{
        category: Category;
    } | undefined>;
    brand: (id: string) => Promise<{
        brand: Brand;
    } | undefined>;
}
