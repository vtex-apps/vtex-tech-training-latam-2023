import { JanusClient } from './JanusClient';
export declare class Session extends JanusClient {
    /**
     * Get the session data using the given token
     */
    getSession: (token: string, items: string[]) => Promise<{
        sessionData: any;
        sessionToken: string;
    }>;
    /**
     * Update the public portion of this session
     */
    updateSession: (key: string, value: any, items: string[], token: any) => Promise<void>;
}
