"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tracing_1 = require("../../tracing");
exports.injectErrorOnSpan = (span, err) => {
    let errorReport;
    if (!(err instanceof tracing_1.ErrorReport)) {
        errorReport = tracing_1.ErrorReport.create({
            message: err.message,
            originalError: err,
        });
    }
    else {
        errorReport = err;
    }
    errorReport.injectOnSpan(span);
};
