import { IOClients } from '../../../../../clients/IOClients';
import { VaryHeaders } from '../../../../../constants';
import { ParamsContext, RecorderState, ServiceContext } from '../../typings';
export interface CachingStrategy {
    forbidden: VaryHeaders[];
    path: string;
    vary: VaryHeaders[];
}
export declare const cachingStrategies: CachingStrategy[];
export declare function cdnNormalizer<T extends IOClients, U extends RecorderState, V extends ParamsContext>(ctx: ServiceContext<T, U, V>, next: () => Promise<void>): Promise<void>;
